#include <stdio.h>
void swapM(int *x, int *y) {
  int temp = *x;
  *x = *y;
  *y = temp;
}

int main() {
    int v[] = {1, 2, 3, 4, 5};
  //   int x = 3, y = 5;
  //   int x_orig = x;
  //   int y_orig = y;
  //   swapM(&x, &y);
  //   printf("antes (%d,%d) e depois (%d,%d)\n", x_orig, y_orig, x, y);

}